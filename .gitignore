# Node.js / Frontend (Vite/React) related files
node_modules/
.npm/
.yarn/
.pnpm-store/
.temp/
dist/
build/
.env
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vite/

# Python / Backend (Flask) related files
venv/
__pycache__/
*.pyc
*.sqlite3
*.db
.flaskenv # You're using this for env vars, but it's often good to explicitly ignore if it contains sensitive data
.python-version # Consider committing this if all team members use pyenv for this project, otherwise ignore. Default to ignore if unsure.

# Editor and OS specific files (Common)
.idea/
.DS_Store
.Trashes
Thumbs.db